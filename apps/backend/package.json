{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": ".venv\\Scripts\\activate.bat && uvicorn app.main:app --reload --port 8000",
    "start": ".venv\\Scripts\\activate.bat && uvicorn app.main:app --host 0.0.0.0 --port 8000"
  }
}
